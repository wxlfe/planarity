name: Deploy Flutter Web to Vercel

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Install dependencies
        run: flutter pub get

      - name: Build Flutter web
        run: flutter build web

      - name: Prepare Vercel routing files
        run: |
          cp vercel.json build/web/vercel.json
          mkdir -p build/web/.well-known
          cp web/.well-known/apple-app-site-association build/web/.well-known/apple-app-site-association
          cp web/.well-known/assetlinks.json build/web/.well-known/assetlinks.json

      - name: Install Vercel CLI
        run: npm install --global vercel@latest

      - name: Deploy Preview
        if: github.event_name == 'pull_request'
        run: vercel --cwd build/web deploy --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}

      - name: Deploy Production
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: vercel --cwd build/web deploy --prod --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID }}
